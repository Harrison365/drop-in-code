const footballTeams = ["Liverpool", "Manchester United", "Chelsea", "Arsenal"];
const marioKart = ["Mario", "Luigi", "Peach", "Bowser"];
const northcoders = ["Kev", "Emily", "Dave", "Paul"];
const shortArray = ["alone"];

console.log(shortArray[0]);

function returnThe3rdElement(array) {
  //gives the third element in the array
}

console.log(returnThe3rdElement(shortArray));

//^^^ use an if statement to handle cases where the array has a length less than 3 ^^^

function returnIndex(array, index) {
  //gives the specified element in the array
}

console.log(returnIndex(northcoders, 0));
